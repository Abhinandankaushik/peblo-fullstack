services:
  backend:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./backend:/app
      - /app/node_modules
      - ./backend/peblo.sqlite:/app/peblo.sqlite
    ports:
      - '4000:4000'
    env_file:
      - ./backend/.env
    command: sh -c "npm install && npm run dev"
